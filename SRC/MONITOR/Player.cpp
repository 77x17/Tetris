#include "Player.hpp"

Player::Player(WINDOW* win):Monitor(win){}

void Player::start() {
    int cnt = 10; 

    // while (cnt) {
    //     if (moveProcessing()) {
            // cnt--;
            // cur->draw(screenPlayer, '#');
    //         continue;
    //     }
    // }
}